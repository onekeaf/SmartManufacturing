spring:
  application:
    name: smart-manufact-inventory
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        namespace: smart-manufact
        group: DEFAULT_GROUP
